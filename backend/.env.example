PORT=5000
NODE_ENV=development

# Optional: enables real Gemini-based hiring-confidence scoring.
# Without it, a deterministic heuristic scorer is used instead.
GEMINI_API_KEY=

# Optional: enables live news search via Serper.dev.
# Without it, the bundled seed dataset is used.
SERPER_API_KEY=
